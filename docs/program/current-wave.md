# Current wave

- Active wave: `publication-governance-wave` after next-20 deterministic local closure
- Status: GitHub CLI authentication and stakeholder-circus visibility are available; publication remains intentionally gated through `stakeholder release ... --execute`
- The original ten validated wider-matrix repos remain publication-held until guarded remote publication and first CI evidence are deliberately executed.
- The next-20 deterministic first-tranche sprint is now 20/20 locally validated with native plus Docker evidence.
- `stakeholder-manager` is installed as the local Rust/Ratatui operator tool for safe status, horizon, feature matrix, single-target actions, and guarded release flow.
- Active live-provider/runtime lanes remain `rust-stakeholder`, `javascript-stakeholder`, and `java-stakeholder`; full live-provider support for all other languages remains the second-pass provider wave.
- The planning horizon now tracks 250 languages. Horizon-only languages are research candidates; they are not managed repositories until a factory dry-run is reviewed and an explicit local scaffold command is executed.
